import { type Dispute } from '../schema';

export async function getAllDisputes(): Promise<Dispute[]> {
    // This is a placeholder declaration! Real code should be implemented here.
    // The goal of this handler is fetching all disputes for Admin Kelurahan users.
    // Should return all disputes in the system with related user and payment information.
    // Should be restricted to admin users only.
    return Promise.resolve([]);
}